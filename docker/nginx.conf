server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # The Angular router owns the paths, so any unknown path must return the app shell rather than
    # a 404; the router then decides what to render.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
